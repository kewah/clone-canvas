# clone-canvas

Clone a canvas by drawing it into a new one.

## Install

With [npm](http://npmjs.org) do:

```bash
$ npm install clone-canvas --save-dev
```

## Usage

```js
var cloneCanvas = require('clone-canvas');
var canvas = document.getElementBy('inputCanvas');
var clone = cloneCanvas(canvas);
```

## License

MIT
